using System;
using System.Collections.Generic;
using System.IO;
using Newtonsoft.Json;

namespace MazeGenerator.Database
{
    //public static class JsonManager
    //{
    //    public static void UpdateJson(string fileName, MemberRepository member)
    //    {
    //        var data = JsonConvert.DeserializeObject<T>(fileName);
    //        update(data);
    //        File.WriteAllText(fileName, JsonConvert.SerializeObject(data));
    //    }

    //    public static void UpdateJsonList<T>(string fileName, Action<List<T>> update);
    //}
}